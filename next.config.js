/** @type {import('next').NextConfig} */
const nextConfig = {
  // distDir: 'build',
  images: {
    domains:
      ['localhost', 'localhost:5000','http://localhost:5000'] 

  }

}

module.exports = nextConfig
